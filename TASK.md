# TASK.md — مهام جاهزة (أعطها لـ Claude Code عند الحاجة)

## ✅ منجز مسبقًا (لا تكرّره)
- المزامنة السحابية موصّلة في `index.html` (cloudPush/cloudPull) وتعمل بعد تعبئة CONFIG.
- لوحة المتابعة `dashboard.html` جاهزة (تقرأ اليوم + آخر ٧ أيام).
- المخطط جاهز في `SUPABASE_SCHEMA.sql`.

## المطلوب منك أولًا
1. تحقّق أن CONFIG (SUPA_URL/SUPA_KEY/FAMILY_ID) معبّأة بنفس القيم في `index.html` و `dashboard.html`.
2. اختبر محليًا: علّم عنصرًا في القائمة وأضف كوب ماء، ثم تأكد أن صفًّا ظهر/تحدّث في جدول `daily_logs` بـ Supabase.
3. افتح `dashboard.html` وتأكد أنه يعرض نفس البيانات.
4. إن نجح: `git commit` برسالة «ربط Supabase + لوحة المتابعة» ثم `push`.

## أفكار لتطويرات لاحقة (نفّذ فقط عند طلب أحمد)
- **عدّاد أسابيع الحمل** يتحدّث تلقائيًا + «طفلكِ هذا الأسبوع بحجم…».
- **مفكرة أعراض** يومية (غثيان/تعب) تُحفظ في `notes` وتظهر في اللوحة لمراجعة الطبيب.
- **إشعار لأبو مانع** عند انخفاض إكمال اليوم (يتطلب Edge Function + cron).
- **تشديد الأمان** بتحويل الوصول إلى Supabase Auth.

## قواعد ثابتة
- ملف واحد لكل صفحة، بدون framework. حافظ على نظام التصميم في CLAUDE.md.
- لا تمسّ الإهداء ولا التنويه الطبي إلا بطلب صريح.
- لا تنشر قبل موافقة أحمد.
